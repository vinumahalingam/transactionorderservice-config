spring:
  application:
    name: transactionorderservice
    
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
  health:
    mongo:
      enabled: false

logging:
  level:
    com:
      transactionorder: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n"
  file:
    name: logs/transactionorder.log
    path: ./logs
server:
  port: 8080
